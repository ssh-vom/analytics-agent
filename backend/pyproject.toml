[project]
name = "backend"
version = "0.1.0"
description = "An agent aimed to analyze datasets by calling python and SQL tooling"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "duckdb>=1.4.4",
    "fastapi>=0.128.4",
    "uvicorn>=0.40.0",
]
